import { FiltroRubrosPipe } from './filtro-rubros.pipe';

describe('FiltroRubrosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRubrosPipe();
    expect(pipe).toBeTruthy();
  });
});
